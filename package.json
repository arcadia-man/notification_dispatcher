{
    "name": "notification-dispatcher-ts",
    "version": "1.0.0",
    "description": "A rule-based notification dispatcher in TypeScript",
    "main": "dist/index.js",
    "scripts": {
        "build": "tsc",
        "start": "npm run build && node dist/index.js",
        "dev": "ts-node src/index.ts",
        "test": "npm run build && jest"
    },
    "devDependencies": {
        "@types/jest": "^29.5.14",
        "@types/node": "^20.12.12",
        "jest": "^29.7.0",
        "ts-jest": "^29.4.4",
        "ts-node": "^10.9.2",
        "typescript": "^5.4.5"
    },
    "dependencies": {
        "json-rules-engine": "^7.3.1"
    }
}
